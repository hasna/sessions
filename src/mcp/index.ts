#!/usr/bin/env bun
// MCP server stub — the full MCP server is in the existing @hasna/sessions package.
// This file will be expanded to include relocate/transfer MCP tools.
console.error("MCP server: use the existing @hasna/sessions package for now");
process.exit(1);
